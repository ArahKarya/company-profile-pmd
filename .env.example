# Postgres connection string. Required in production; when it is unset the site falls back
# to the bundled default content in src/content/ and the admin panel is unavailable.
DATABASE_URL="postgresql://user:password@localhost:5432/pangan_masa_depan?schema=public"
